#ifndef _MEMORY_H
#define _MEMORY_H

////////////////////////////////////////////////////////////
// Headers
////////////////////////////////////////////////////////////
#include <stdio.h>
#include <stdlib.h>

////////////////////////////////////////////////////////////
// Copy memory macro
////////////////////////////////////////////////////////////
#define CopyMemory(Dest, Src, Size) (memcpy(Dest, Src, Size))

////////////////////////////////////////////////////////////
// Copy memory macro
////////////////////////////////////////////////////////////
#define AllocateMemory(Type, Value) ((Type *) malloc(Value))

////////////////////////////////////////////////////////////
// Free memory macro
////////////////////////////////////////////////////////////
#define FreeMemory(Pointer) (free(Pointer))

#endif